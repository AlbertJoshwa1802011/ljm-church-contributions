# ADR NNNN: <short, specific title — not "Fund changes", but "Fund visibility scoping via members-only funds">

**Status:** Proposed | Accepted | Superseded by ADR-NNNN | Deprecated

**Date:** YYYY-MM-DD

## Context

What situation makes a decision necessary? What constraints apply (frozen
giving path? additive-only schema? no-build-step frontend? existing
patterns this must fit)? Link the relevant
[`../../architecture/`](../../architecture/) document(s) this touches.

## Problem

The specific question being decided, stated so a future reader who wasn't in
the conversation can understand what was actually being resolved.

## Decision

What was decided, stated plainly. If this changes or extends an existing
architecture document, say which one and update it in the same change.

## Alternatives considered

What else was on the table, and why it wasn't chosen. A one-line dismissal
is fine if the reason is obvious; give real reasoning for anything a
reasonable future agent might otherwise re-propose.

## Consequences

What gets easier, what gets harder, what new constraints this creates for
future work. Be honest about downsides — an ADR that only lists upsides
isn't trustworthy.

## Migration implications

Does this require a schema migration? Is it additive per `CONTRIBUTING.md`
§4? Does it touch the frozen giving path (`CONTRIBUTING.md` §3)? If yes to
either, what's the rollout/rollback plan?
